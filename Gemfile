source 'https://rubygems.org'
ruby '2.1.2'
#ruby-gemsetd=perry-default

group :development, :test do
  #TODO: Revert guard-rspec to normal guard. We're no longer using rspec.
  gem 'guard-rspec', require: false
  gem 'mocha', '~> 1.1.0'
  gem 'test-unit', '~> 2.5.5'
  gem 'ruby-lint', '~> 2.0.1'
end

group :doc do
end

group :production do
end
